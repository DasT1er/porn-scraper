# Web scraping
beautifulsoup4==4.12.3
requests==2.31.0
# lxml==5.1.0  # Optional - commented out for Windows compatibility
# BeautifulSoup will use html.parser instead (built-in, no compilation needed)

# Browser automation (for JavaScript-heavy sites)
selenium==4.16.0
# webdriver-manager==4.0.1  # Replaced with undetected-chromedriver
undetected-chromedriver>=3.5.4  # More stable, better anti-detection

# Playwright - Optional, modern browser automation (commented out - optional)
# playwright>=1.40.0  # Uncomment if you want to use Playwright instead of Selenium

# HTTP requests (async)
httpx==0.26.0

# UI and CLI
rich==13.7.0
click==8.1.7
questionary==2.0.1  # Interactive terminal UI

# Image processing (OPTIONAL - for image dimension validation)
# Scraper works without Pillow but will only validate file size, not dimensions
# If installation fails on Windows, the scraper will still work fine
# Pillow==10.2.0  # Uncomment if you want full image validation

# Configuration
pyyaml==6.0.1
python-dotenv==1.0.1

# Utilities
tqdm==4.66.1
validators==0.22.0
